export function addCalc(a: number, b: number): number {
    return a + b;
}

export function subtractCalc(a: number, b: number): number {
    return a - b;
}

export function multiplyCalc(a: number, b: number): number {
    return a * b;
}

// Add more functions for multiplication, division, etc.